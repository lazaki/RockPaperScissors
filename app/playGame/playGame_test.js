'use strict';

describe('RockPaperScissors.playGameCtrl module', function() {

  beforeEach(module('RockPaperScissors.playGameCtrl'));

  describe('playGameCtrl controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var playGame = $controller('playGameCtrl');
      expect(playGame).toBeDefined();
    }));

  });
});